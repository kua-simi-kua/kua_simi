# action.yml
name: 'Run Docker Image'
description: 'Docker test'
inputs:
  token: 
    description: 'Github Token'
    required: true
outputs:
  results: # id of output
    description: 'results'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - '${{ inputs.token }}'
